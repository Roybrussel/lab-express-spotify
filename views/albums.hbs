<div class="albums">
    {{#each album}}
    <p>{{this.name}}</p>
    <p>{{this.images.0.url}}</p>
    <img src="{{this.images.0.url}}">
    <a href="/tracks/{{this.id}}">Listen to tracks</a>
    {{/each}}
</div>